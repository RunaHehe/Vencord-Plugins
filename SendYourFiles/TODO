(too lazy to make an actual todo)

-Fix the network requests (currently unable to upload files)
-Polish the upload ui (it looks reallyyyy terrible rn)
-Polish the code
-Replace Upload UI with an IFrame